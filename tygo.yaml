packages:
  - path: github.com/fnune/kyaraben/internal/model
    output_path: ui/types/model.gen.ts
    include_files:
      - system.go
      - emulator_id.go
      - frontend_id.go
      - hotkey_mapping.go
    enum_style: union

  - path: github.com/fnune/kyaraben/internal/logging
    output_path: ui/types/logging.gen.ts
    include_files:
      - types.go
    enum_style: union

  - path: github.com/fnune/kyaraben/internal/daemon
    output_path: ui/types/daemon.gen.ts
    include_files:
      - types.go
      - protocol.go
    enum_style: union
    type_mappings:
      model.SystemID: SystemID
      model.EmulatorID: EmulatorID
      model.FrontendID: FrontendID
      model.Manufacturer: Manufacturer
      model.HotkeyID: HotkeyID
      logging.LogEntry: LogEntry
    frontmatter: |
      import type { SystemID, EmulatorID, FrontendID, Manufacturer, HotkeyID } from './model.gen'
      import type { LogEntry } from './logging.gen'
